<!doctype html>
<html lang="{{ metadata.lang }}">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title }}</title>
    {% include "head.njk" %}
  </head>
  <body>
    {{ content | safe }}
  </body>
</html>